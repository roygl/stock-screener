streamlit>=1.40
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26
pyarrow>=15  # Parquet engine for the local price cache (screener/cache.py)
pytest>=8  # optional test runner (suite also runs via: python tests/test_indicators.py)
anthropic>=0.40  # optional: enables the LLM-backed NL agent; without it (or without ANTHROPIC_API_KEY) the agent degrades to the offline rule-based parser
